import 'package:flutter/material.dart';

class VaccineAddPage extends StatelessWidget {
  const VaccineAddPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Vaccine Add"),
      ),
      body: Center(
        child: Text("Vaccine Add Page"),
      ),
    );
  }
}
